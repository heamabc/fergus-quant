import * as React from 'react';
import './GradientLine.scss';

const GradientLine = (props) => (
    <div style={{width : props.width, height:2}} className="line" ></div>
);

export default GradientLine;